<%= form_with(model: study, local: true, html: { class: 'border p-4 col-md-4 justify-content-center' }) do |form| %>
  <% if study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(study.errors.count, "error") %> prohibited this study from being saved:</h2>

      <ul>
        <% study.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :study_name %>
    <%= form.text_field :study_name, class: 'form-control', required: true %>
  </div>

  <div class="form-group">
    <%= form.label :direction %>
    <%= form.select :direction, options_for_select(['MAXIMIZE', 'MINIMIZE']), {}, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
